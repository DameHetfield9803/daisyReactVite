export default function Fallback() {
    return (
        <main className="text-center font-thin mx-auto container flex flex-col p-6 text-wrap bg-black">
            <h1><strong className="text-transparent bg-clip-text bg-gradient-to-r from-purple-500 via-pink-500 to-orange-500">Please wait while we are loading...</strong></h1>
        </main>
    );
}